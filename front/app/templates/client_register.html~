{% extends "base.html" %}
{% import "bootstrap/wtf.html" as wtf %}

{% block body %}
{{super() }}
<body background="{{ url_for('static', filename='img/regpic3.jpg') }}"></body>

<style>
	.jumbotron {
	    border-radius: 30px;
	    background-color: #F1F1FA;
	}
</style>
{% endblock body %}


{% block base_content %}
<div class="container" style="margin-top:20px">
 <div class="col-md-8 col-md-offset-2 centerfy">
   
	<div class="jumbotron">
	<div class="container">
	   <div class="col-md-8 col-md-offset-2 centerfy">
		<h2><center>Client Registration</center></h2><br>
		{% if error_message %}
				  <div class="bs-example">
				    <div class="alert alert-danger alert-error">
					<a href="#" class="close" data-dismiss="alert">&times;</a>
					<strong>Error!</strong> {{error_message}}
				    </div>
				  </div>
		{% endif %}
		<form method="POST" action="{{ url_for('main.client_register') }}" id="form">
			 {{ wtf.quick_form(form, form_type='small') }}
			 {{ form.csrf_token }}
			 <div align=center><button type="submit" form="form" value="Submit" class="btn btn-primary">Sign up!</button>
		</form>
		<br>
		<br>
	   </div>
	</div>
   </div>
 </div>
</div>


{% endblock base_content %}
