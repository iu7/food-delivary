{% extends "base.html" %}
{% import "bootstrap/wtf.html" as wtf %}

{% block body %}
{{ super() }}
<body background="{{ url_for('static', filename='img/background.jpg') }}"></body>
<style>
	.jumbotron {
	    border-radius: 15px;
	}

	td
	{
	    padding:0 10px 0 20px;
	}
	.list-group-item{
	    
	    border-radius: 30px;
	    background-color:#CCCCDF;
	    width: 100%;
	}
	.glyphicon-plus{
		font-size: 25px;
		color: #FFFFFF;
	}
	.glyphicon-trash{
		font-size: 20px;
	}

	.glyphicon-pencil{
		font-size: 20px;
	}

</style>

{% endblock body %}

{%block base_content %}

<hr class="featurette-divider">

	<div class="col-md-8 col-md-offset-2 centerfy">
	    <div class="jumbotron" style="background: #C0C0C0;">
	      <div class="container">
		     <div class="col-md-8 col-md-offset-4 centerfy">
		        <h3 class=""><font color=#008080><b><i>Restaurant menu item edit</i></b></font></h3>
			
		     </div>
		   
		     <div class="col-md-8 col-md-offset-2 centerfy">{#11111#}
			<hr class="featurette-divider">
			 {% if error_message %}
			  <div class="bs-example">
			    <div class="alert alert-danger alert-error">
				<a href="#" class="close" data-dismiss="alert">&times;</a>
				<strong>Error!</strong> {{error_message}}
			    </div>
			  </div>
		     {% elif result_message %}
			<div class="alert alert-success">
  				  <a href="#" class="close" data-dismiss="alert">&times;</a>
  				  <strong>Success!</strong> {{ result_message }}
			</div>
		     {% endif %}

			<form method="POST" action="{{ url_for('main.restaurant_menu_update',name=user['name'],menu_id=menu_id,
				restaurant_id=restaurant_id,menu_item_id=menu_item_id) }}" id="form">
				{{ form.hidden_tag() }}
				{{ wtf.form_field(form.title) }}
				{{ wtf.form_field(form.price) }}
				{{ wtf.form_field(form.info) }}
				<div align=center>
				 <button type="submit" class="btn btn-primary" id="form">Update</button>
				</div>
			 </form>		 
			
		    </div>{#11111#}

	      </div>{#container#}
		<br>
	    </div>{#jumbotron#}
	</div>{#col-md#}
{%endblock base_content %}




